export const requestData = async (endpoint) => {
  // const { data } = await get(endpoint); esboco
  return data;
};

export const requestLogin = async (endpoint, body) => {
  // const { data } = await post(endpoint, body); esboco
  return data;
};